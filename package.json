{
  "name": "liftlab-root",
  "version": "1.3.0",
  "description": "Root package runner for LiftLab Next.js project",
  "private": true,
  "scripts": {
    "dev": "npm run dev --prefix LiftLab",
    "build": "npm install --prefix LiftLab && npm run build --prefix LiftLab",
    "start": "npm run start --prefix LiftLab",
    "lint": "npm run lint --prefix LiftLab"
  }
}